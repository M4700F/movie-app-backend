spring.jpa.properties.hibernate.statement_cache.size=0
spring.datasource.hikari.data-source-properties.prepareThreshold=0
spring.application.name=movie-app-backend

server.port=8080

#spring.datasource.url={DB-URL}
#spring.datasource.username={DB-USERNAME}
#spring.datasource.password={DB-PASSWORD}

spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=validate
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.initialization-fail-timeout=1
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.profiles.active=local

#swagger url : http://localhost:8080/swagger-ui/index.html

# application.properties
# tells Hibernate not to cache named prepared statements ? eliminating the reuse conflict.
# This makes the driver send plain SQL instead of PREPARE/EXECUTE commands.

server.address=0.0.0.0
